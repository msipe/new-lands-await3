{
  "name": "new-lands-await3",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tstl -p tsconfig.json && node scripts/sync-lua.mjs",
    "watch": "tstl -p tsconfig.json -w",
    "dev": "npm run build && npm run watch",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@typescript-to-lua/language-extensions": "^1.19.0",
    "@types/jest": "^30.0.0",
    "jest": "^30.1.3",
    "love-typescript-definitions": "^11.5.0",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.2",
    "typescript-to-lua": "^1.31.0"
  }
}