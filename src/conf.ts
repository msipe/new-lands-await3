love.conf = (config) => {
    config.identity = "new-lands-await3";
    config.version = "11.5";

    config.window.title = "new-lands-await3";
    config.window.width = 960;
    config.window.height = 540;
    config.window.resizable = false;
};